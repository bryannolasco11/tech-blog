<article>
    <div>
        <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
        {{text_content}}
    </div>

    <div>
        Posted by {{user.username}} on {{created_at}}
    </div>
</article>