<article>
    <div>
        <h4><a class="link" href="/post/{{id}}">{{title}}</a></h4>
        {{text_content}}
    </div>

    <small>
        Posted by {{user.username}} on {{format_date created_at}}
    </small>
</article>